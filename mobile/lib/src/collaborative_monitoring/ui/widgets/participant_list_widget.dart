import 'package:flutter/material.dart';

class ParticipantListWidget extends StatelessWidget {
  const ParticipantListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Participant List Widget'),
    );
  }
}
