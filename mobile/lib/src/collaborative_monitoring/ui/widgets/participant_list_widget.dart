import 'package:flutter/material.dart';

class ParticipantListWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Participant List Widget'),
    );
  }
}
