import 'package:flutter/material.dart';

class CommunityRatingWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Community Rating Widget'),
    );
  }
}
