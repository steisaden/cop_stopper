import 'package:flutter/material.dart';

class CommunityRatingWidget extends StatelessWidget {
  const CommunityRatingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Community Rating Widget'),
    );
  }
}
