import 'package:flutter/material.dart';

class SpectatorModeSettingsWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Spectator Mode Settings Widget'),
    );
  }
}
