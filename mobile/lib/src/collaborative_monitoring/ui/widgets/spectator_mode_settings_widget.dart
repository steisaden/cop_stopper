import 'package:flutter/material.dart';

class SpectatorModeSettingsWidget extends StatelessWidget {
  const SpectatorModeSettingsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Spectator Mode Settings Widget'),
    );
  }
}
