import 'package:flutter/material.dart';

class ComplaintHistoryWidget extends StatelessWidget {
  const ComplaintHistoryWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Complaint History Widget'),
    );
  }
}
