import 'package:flutter/material.dart';

class ComplaintHistoryWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Complaint History Widget'),
    );
  }
}
